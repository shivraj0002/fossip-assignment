import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#31CFFC',
        },
        colors: {
            lgrey: '#f8f8f8',
            dgrey: '#171717,'
        },
    },

})

export default theme